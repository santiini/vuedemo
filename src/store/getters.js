/**
 * getters;
 */
const getters = {
    header: state => state.header,
    toast: state => state.toast,
    phoneUserId: state => state.phoneUserId,
    route: state => state.route
};

export default getters;